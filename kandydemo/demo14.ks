from testlib import imath
from testlib import imath2
python import hashlib


if KANDY_MAIN{ #Verify if this is the main-file.

    print(imath.sumar(2, 3))
    print(imath2.restar(2, 3))
    print(imath.imath2.restar(2, 9))
    print(hashlib)

}
